fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
psycopg2-binary==2.9.9
# SQLAlchemy не пинуна, но её минорные обновления меняли драйвер по умолчанию
# для схемы postgresql:// с psycopg2 на psycopg (v3), которого в requirements
# нет — и preDeploy-миграция падала на ModuleNotFoundError. Драйвер задаём
# явно, чтобы выбор не зависел от версии SQLAlchemy.
SQLAlchemy==2.0.52
python-dotenv==1.0.1
requests==2.32.3
python-jose[cryptography]==3.3.0
# Fernet + HKDF для шифрования токенов соцсетей в базе. Приходил транзитивно
# через python-jose, но зависимость теперь прямая — пинуем явно.
cryptography==50.0.0
openpyxl==3.1.2
bcrypt==4.1.3
alembic==1.16.5